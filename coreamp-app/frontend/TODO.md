# CoreAmp Vue UI

The Vue/VibeUI migration is complete — see the top of `CHANGELOG.md`
(`[Unreleased]`) for what shipped.

Outstanding work (frontend gaps, games roadmap, audiophile/post-parity, cutover)
now lives in the root `../../TODO.md`.
